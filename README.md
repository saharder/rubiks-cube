# Rubiks Cube Renderer

This repo houses code for rendering a 3d rubiks cube via three.js. 

## Testing Changes

Since this is a library for rendering a 3d rubiks cube, there is a test site included with the package 
for testing the rubiks cube. This can be launched via the `npm run dev` command. 
